fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic[email]==2.10.4
PyJWT==2.10.1
bcrypt==4.2.1
python-multipart==0.0.20

# PostgreSQL driver. Only used when DATABASE_URL is set (i.e. when hosted).
# Locally the app uses SQLite and never touches this.
psycopg[binary]==3.2.3
